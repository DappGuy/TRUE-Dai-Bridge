{
  "name": "true-dai-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "True-Dai token bridge service",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm run lint && npm run clean && tsc",
    "test": "tsc && mocha dist/**/*.spec.js",
    "lint": "tslint src/**/*.ts",
    "clean": "rm -rf ./dist || rmdir /s/q dist"
  },
  "author": "Felix",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.5",
    "@types/express": "^4.16.1",
    "commander": "^2.20.0",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "level": "^5.0.1",
    "web3true": "^1.1.6"
  },
  "devDependencies": {
    "@types/node": "^12.0.2",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "engines": {
    "node": ">= 8.0.0",
    "npm": ">=5.2.0"
  }
}
